-- HTTP library --

local http = package.loaded.http
-- native http.request: function(
--  url:string[, post:string[, headers:table[, binarymode:boolean]]])
-- post is the data to POST.  Otherwise a POST is sent.
